@model IEnumerable<projectWebKassa.Models.IndexPrijsViewModels>
@{
    ViewBag.Title = "PrijsManagmentView";
}

<h2>PrijsManagmentView</h2>

<h4>Verkoop prijs</h4>
<p>@Html.DropDownList("prijslijst", "prijslijst")</p>
<p>@Html.ActionLink("Create new", "create")</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.startDatum)
        </th>
        <th>
            Prijs
        </th>
        <th>
            BTW
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td></td>
        </tr>
    }
</table>