@model IEnumerable<projectWebKassa.DAL.Filiaal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adre.Straat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adre.Straat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FiliaalCodeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FiliaalCodeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FiliaalCodeId })
        </td>
    </tr>
}
</table>