@model IEnumerable<projectWebKassa.DAL.order>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.personeelId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderDatum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LeveringsDatum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.personeelId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderDatum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LeveringsDatum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.orderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.orderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.orderId })
        </td>
    </tr>
}
</table>